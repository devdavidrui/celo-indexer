# celo-indexer
Indexer for getting transactions for m2fl-apf

# Usage
Use contracts files — contract1.md, contract2.md, contract3.md.
    const BLOCKS = [];
    const DEPLOYED_FLASHLOAN_CONTRACTS = [""];